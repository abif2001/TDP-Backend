function Information(props) {
    return (
        <div>
            Name:<b> {props.name}</b>
            <br/>
            Address:<b> {props.address}</b>
        </div>
    )
}

export default Information;